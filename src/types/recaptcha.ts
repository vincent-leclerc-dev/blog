export type TokenProperties = {
  action: string;
  androidPackageName: string;
  createTime: string;
  hostname: string;
  invalidReason: string;
  iosBundleId: string;
  valid: boolean;
};
